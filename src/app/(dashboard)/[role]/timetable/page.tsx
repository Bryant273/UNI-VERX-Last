import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

export default function TimetablePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Emploi du Temps</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">La page de l'emploi du temps est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
